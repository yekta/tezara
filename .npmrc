# Next.js and its plugins resolve some deps by walking node_modules; pnpm's strict
# isolation breaks that unless we hoist them.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
node-linker=hoisted
